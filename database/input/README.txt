sectors.zip is the immutable completed Kane Offline Map classification release.

It contains the 16 accepted legacy sector-state JSON documents used only as the migration source for the GeoPackage database. Do not edit files inside this archive.

Build the SQL candidate on Linux with:

  bash database/build-ledger-database.sh
