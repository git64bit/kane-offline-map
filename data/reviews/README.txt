Generated open-review bundles are external data products and are not committed.

The browser default is data/reviews/current. On Linux, that path may be a symlink
to an accepted bundle outside the repository. For final offline packaging, copy the
accepted bundle directory to data/reviews/current as ordinary files.
